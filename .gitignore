bin/
keys/
.env
*.exe

# Ключи и локальные данные стенда
pgdata/

# Node
node_modules/

# Python
__pycache__/
*.pyc

# Собранная спека — производный артефакт: пересобирается из paths/ и shared/
# командой `task spec`. В репозитории лежит только источник, разнесённый по
# доменам, плюс сгенерированные из него типы (их коммитим — компилятор Go
# и сборка фронта не должны требовать установленного генератора).
api/bundle.yaml
*.tmp.yaml
api/docs.html
api/build/
go.work.sum
packages/contract-ts/node_modules/

# macOS
.DS_Store
